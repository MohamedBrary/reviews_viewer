= form_for @review, html: { class: "form-horizontal", role: "form" } do |f|
  -if @review.errors.any?
    .alert.alert-danger.alert-dismissable{role: "alert"}
      %button.close{type: "button", data: {dismiss: "alert"} }
        %span{aria: {hidden: "true"} } &times;
        %span.sr-only Close
      %h4= "#{pluralize(@review.errors.count, "error")} prohibited this review from being saved:"

      %ul
        - @review.errors.full_messages.each do |msg|
          %li= msg

  .form-group
    = f.label :comment, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :comment, class: 'form-control'
  .form-group
    = f.label :posted_at, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :posted_at, class: 'form-control'
  .form-group
    = f.label :theme_ids, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.number_field :theme_ids, class: 'form-control'
  .form-group
    = f.label :category_ids, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.number_field :category_ids, class: 'form-control'
  .form-group
    .col-sm-offset-2.col-sm-10
      = f.submit class: 'btn btn-primary'
